---
title: What is Dash Platform?
description: See what Dash Platform lets you build, and what you will build here.
module: 2
tier: concepts
estimatedMinutes: 12
exp: 100
verification: quiz
prerequisites: [1]
---

In the previous lesson you met Dash the money. This lesson introduces the other half of the same network: Dash Platform, the layer that gives your application somewhere to keep its data.

## One network, two jobs

Dash is one network organized as two layers, each focused on its own job. You already met the first:

- **Dash Core** is the layer-1 money layer. It settles Dash payments and carries the currency itself.
- **Dash Platform** is the layer-2 application-data layer. It stores structured application data — the records behind usernames, profiles, listings, and other app state — and verifies that only authorized operations change it.

The relationship runs one way: Platform can read what is on Core, but Core does not depend on Platform. Splitting the network this way keeps the money layer focused on money while the data layer handles everything an application needs to keep.

<Callout type="info" title="A useful mental model">
Core is the payment rail. Platform is the database. One network, two jobs.
</Callout>

## How Platform stores your data

Most blockchains add application logic by letting you deploy a <Term id="smart-contract">smart contract</Term> — program code the network runs for you. Platform takes the opposite approach: there is no contract code of your own to upload or run. Instead, you **describe** your data, and the network stores and validates it.

Three ideas carry the whole thing:

1. **A data contract describes the shape of your data.** It is a schema — a JSON Schema document that declares the kinds of records your app stores, with their fields and types. It is the same idea as defining a schema for a document-oriented database such as MongoDB: you say what a record may contain, and the network holds you to it.
2. **Documents are the records.** Each record your app actually stores is a document, in the same sense as a row in a table or an object in a database. Every document must conform to the data contract it belongs to, and each one is owned by an identity.
3. **State transitions are the only way data changes.** Nothing writes to Platform directly. Every create, update, or delete is a signed state transition from the identity that owns the record. The network checks the change against the contract, and only then records it.

Here is the smallest possible taste of idea one. A note-taking app might describe its records like this:

```json
{
  "note": {
    "type": "object",
    "properties": {
      "message": { "type": "string", "position": 0 }
    },
    "required": ["message"],
    "additionalProperties": false
  }
}
```

This is a declaration, not code. (The `position` entry is an ordering detail you will meet when you write contracts yourself later on — for now, notice only the shape.) Hold onto the pattern: **declare the data, submit signed changes, let the network validate and store them.**

Two components do the storing and serving:

- **Drive** is Platform's decentralized storage. It validates each signed change and keeps the network's copy of every app's data in <Term id="grovedb">GroveDB</Term>, a storage engine that can hand back cryptographic proofs with an answer to a query.
- **DAPI** — the decentralized API — is how your app talks to all of it. It exposes <Term id="json-rpc">JSON-RPC</Term> and <Term id="grpc">gRPC</Term> endpoints, so a client can read and write Platform data (and query Core) through the SDK without running a node of its own.

<Callout type="info" title="Why the proofs matter">
Because GroveDB answers come with proofs, your app can check that the data it reads is genuinely the network's data — not something a single server made up. You do not have to trust the node you queried; you verify what it returns.
</Callout>

## What you can build

With storage, identities, and signed writes in place, the same primitives combine into many different applications. Concrete things developers build on Platform include:

- **Usernames.** The Dash Platform Name Service (DPNS) registers human-readable `.dash` names like `alice.dash` and ties each one to a Platform identity — a persistent account that owns data and signs the changes to it. DPNS is itself just an app built on Platform.
- **Shared application data.** Publish a data contract and submit documents, so your users share verifiable records — profiles, posts, listings — instead of trusting one company's database.
- **Tokens.** Create configurable fungible tokens (interchangeable, one-like-another units) for rewards, memberships, or loyalty points, without deploying contract code.
- **NFTs.** Build unique, document-based non-fungible tokens for collectibles and digital goods — each one is a document with a unique ID, with transfer and purchase built in.
- **Payments by name.** DashPay, Dash's flagship wallet, sends and receives money addressed to a username instead of a bare address by building on Platform's identities and DPNS names.

The documentation's own tutorials build working examples with these pieces: **Dashnote** (a small notes app), **DashMint Lab** (an NFT marketplace), and **DashPay**.

Notice what is absent from the list: a marketplace for arbitrary user-deployed programs. Platform gives you storage, validation, and signed operations — it does not run unrestricted code on every node. You design the data and the rules around it in your own application code.

## What you will build in this course

The rest of the course is hands-on. Working entirely on <Term id="testnet">testnet</Term> — the parallel copy of the network that runs on free coins — you will assemble one small application from end to end:

1. Connect to DAPI and make a verified read.
2. Derive and fund a Platform address.
3. Create a Platform identity.
4. Register a DPNS username.
5. Publish a data contract for your app's records.
6. Submit and query documents.
7. Transfer a document and run a purchase flow.
8. Create a native token and charge tokens for document operations.

By the final module you will have touched every layer introduced here — the Core payment rail, identities, names, a data contract, documents, and a token — all without running a node of your own. Each hands-on lesson builds one piece; together they make one working app.

## Checkpoint

Pass the quiz to complete the lesson.

<LessonQuiz
  challengeId="what-is-dash-platform"
  passingScore={4}
  questions={[
    {
      id: "layer-split",
      prompt: "Which description correctly separates Dash Core from Dash Platform?",
      options: [
        { id: "a", label: "Core stores application records, while Platform mines Dash payments." },
        { id: "b", label: "Core is the layer-1 money layer; Platform is the layer-2 application-data layer that stores and verifies records." },
        { id: "c", label: "Core and Platform are two names for the same chain and data store." },
        { id: "d", label: "Platform is a private database that only Core nodes may query." },
      ],
      correctOptionId: "b",
      explanation: "Core settles Dash payments on layer 1. Platform stores and verifies structured application data on layer 2.",
    },
    {
      id: "schema-not-code",
      prompt: "How does an application store data on Dash Platform?",
      options: [
        { id: "a", label: "It deploys custom code the network runs on every write." },
        { id: "b", label: "It describes records with a schema (a data contract) and submits signed changes; the network validates and stores them." },
        { id: "c", label: "It writes directly into a single company's database through DAPI." },
        { id: "d", label: "It uploads unvalidated JSON that any node may edit at will." },
      ],
      correctOptionId: "b",
      explanation: "A data contract declares the shape of the data, and signed state transitions change it. There is no contract code for the network to execute.",
    },
    {
      id: "change-mechanism",
      prompt: "What is the only way a Platform record can change?",
      options: [
        { id: "a", label: "Anyone holding the app's URL can edit any record directly." },
        { id: "b", label: "A signed state transition from the identity that owns the record, checked against the data contract." },
        { id: "c", label: "The developer rewrites records on a read replica and reproduces them on demand." },
        { id: "d", label: "A nightly batch job that reconciles rows against a local cache." },
      ],
      correctOptionId: "b",
      explanation: "Nothing writes to Platform directly. Every create, update, or delete is a signed state transition that the network validates against the contract before recording.",
    },
    {
      id: "buildables",
      prompt: "Which is NOT something you can build on Dash Platform today?",
      options: [
        { id: "a", label: "Human-readable .dash usernames tied to an identity." },
        { id: "b", label: "A marketplace for arbitrary user-deployed code that runs on every node." },
        { id: "c", label: "Shared document-backed app data, such as profiles, posts, or listings." },
        { id: "d", label: "Configurable fungible tokens and document-based NFTs." },
      ],
      correctOptionId: "b",
      explanation: "Platform gives storage, validation, and signed operations — not a place to run unrestricted user-written code. That is the trade it makes versus a general-purpose smart-contract chain.",
    },
    {
      id: "course-arc",
      prompt: "What will you build over the rest of this course?",
      options: [
        { id: "a", label: "A single end-to-end testnet app: connect to DAPI, create an identity, register a username, publish a contract, submit and query documents, and create a token." },
        { id: "b", label: "A replacement layer-1 blockchain that upgrades Dash Core." },
        { id: "c", label: "A collection of unrelated scripts with no shared data." },
        { id: "d", label: "Only payment transactions — no identities, contracts, or documents." },
      ],
      correctOptionId: "a",
      explanation: "Working on testnet, you will assemble identities, names, a data contract, documents, and a token into one small application.",
    },
  ]}
/>

## What you accomplished

You can now separate Dash Core's money job from Dash Platform's data job, and describe how Platform stores data — a schema you declare, documents it accepts, and signed state transitions that change them. You can name the concrete applications that combination makes possible, from `.dash` usernames to tokens and NFTs, and you know the path ahead: a hands-on, testnet build that takes you from your first verified read to a token-charged application.