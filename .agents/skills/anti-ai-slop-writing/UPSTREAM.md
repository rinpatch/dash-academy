# Provenance

Vendored from https://github.com/jalaalrd/anti-ai-slop-writing at commit
`63255f9bbb75a265dc5786a04535cd033f487756` (2026-04-19).

`SKILL.md` and `references/banned-words.md` are copied verbatim so they can be diffed against
upstream when it moves. Put our carve-outs in `write-dash-lesson/SKILL.md` instead of editing
them here.

The repository carries no licence file. It is a prompt document rather than code we ship, but
that is worth knowing before it goes anywhere beyond this repo.

Its rules were written for tweets, emails, and blog posts. A lesson is none of those, and three
of them actively fight the factory's other gates — see "Anti-slop scope" in
`write-dash-lesson/SKILL.md` for which parts do not apply.
